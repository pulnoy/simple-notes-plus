<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network & Sync Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    
    <!-- Notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    
    <!-- Battery Optimization (for WorkManager background sync) -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    
    <!-- Background sync uses ordinary WorkManager jobs, not foreground services. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" tools:node="remove" />
    
    <!-- BOOT Permission - CRITICAL für Auto-Sync nach Neustart! -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- v2.10.0: Biometric app lock -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />

    <!--
      🆕 v2.14.0: Package Visibility (Android 11+). Ohne diese Deklaration bleiben
      installierte Mail-Apps für uns unsichtbar und der mailto-Selector des
      "Log an Entwickler senden"-Buttons löst ins Leere auf. Bewusst eng auf
      mailto beschränkt statt QUERY_ALL_PACKAGES (F-Droid/Play-Compliance).
    -->
    <queries>
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application
        android:name=".SimpleNotesApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SimpleNotes"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <!-- MainActivity v1.5.0 (Jetpack Compose) - Launcher -->
        <activity
            android:name=".ui.main.ComposeMainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.SimpleNotes.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Editor Activity v1.5.0 (Jetpack Compose) -->
        <!-- 🆕 v2.2.0: Share Intent Target für text/plain -->
        <activity
            android:name=".ui.editor.ComposeNoteEditorActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize"
            android:parentActivityName=".ui.main.ComposeMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Settings Activity v1.5.0 (Jetpack Compose) -->
        <!-- v1.8.0: Handle locale changes without recreate for smooth language switching -->
        <activity
            android:name=".ui.settings.ComposeSettingsActivity"
            android:configChanges="locale|layoutDirection"
            android:parentActivityName=".ui.main.ComposeMainActivity" />

        <!-- Boot Receiver - Startet WorkManager nach Reboot -->
        <receiver
            android:name=".sync.BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- FileProvider für Log-Sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Remove WorkManager’s unused foreground service from the merged manifest. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            tools:node="remove" />

        <!-- 🆕 v1.8.0: Widget Receiver -->
        <receiver
            android:name=".widget.NoteWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/note_widget_info" />
        </receiver>

        <!-- v1.8.0: Widget Config Activity -->
        <activity
            android:name=".widget.NoteWidgetConfigActivity"
            android:exported="false"
            android:theme="@style/Theme.SimpleNotes" />

        <!-- Quick-Edit-Overlay über dem Homescreen (RemoteViews kennt kein EditText).
             exported="false" reicht: der PendingIntent entsteht im eigenen Prozess.
             Keine neue <uses-permission> — SYSTEM_ALERT_WINDOW bräuchte nur, wer über
             FREMDE Apps zeichnet.
             taskAffinity="" ist Pflicht: ohne eigenen Task landet das transparente Fenster im
             Task der App, und statt des Homescreens steht die Notizliste dahinter. -->
        <activity
            android:name=".widget.WidgetQuickEditActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/Theme.SimpleNotes.QuickEdit"
            android:windowSoftInputMode="adjustResize" />

        <!-- 🆕 v2.2.0: New-Note Shortcut Widget Receiver -->
        <receiver
            android:name=".widget.NewNoteWidgetReceiver"
            android:exported="true"
            android:label="@string/new_note_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/new_note_widget_info" />
        </receiver>

        <!-- Notes-List Scroll Widget Receiver -->
        <receiver
            android:name=".widget.NotesListWidgetReceiver"
            android:exported="true"
            android:label="@string/notes_list_widget_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/notes_list_widget_info" />
        </receiver>

        <activity
            android:name=".widget.NotesListWidgetConfigActivity"
            android:exported="true"
            android:theme="@style/Theme.SimpleNotes" />

    </application>

</manifest>
