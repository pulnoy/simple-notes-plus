<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- App-internal files directory -->
    <files-path name="logs" path="." />
    <!-- 🆕 v1.10.0-Papa: Cache directory for shared PDF files -->
    <cache-path name="shared_pdfs" path="shared_pdfs/" />
    <!-- 🆕 Bild-Attachments: dedicated entry for sharing note images (ACTION_SEND_MULTIPLE) -->
    <files-path name="assets" path="assets/" />
    <!-- 🆕 v2.14.0: anonymisierte Log-Kopien für den Export (Originale bleiben in files/) -->
    <cache-path name="shared_logs" path="shared_logs/" />
    <!-- Simple Notes+ : photo temporaire remise à l'application Appareil photo. -->
    <cache-path name="camera_photos" path="camera_photos/" />
</paths>
